# Local-only override (not the canonical config).
# Host port 5000 is occupied by another project on this machine,
# so expose the backend on 5001 instead. The frontend still reaches
# the backend via the internal Docker network (service "backend:5000"),
# so this only changes direct host access to the API.
services:
  backend:
    ports: !override
      - "5001:5000"

  # Optional standalone Nginx frontend (`--profile standalone-frontend`). Host
  # port 8080 is taken by another project, so expose it on 8081. The default
  # Compose run serves the same Flutter UI directly from the backend on 5001.
  frontend:
    build:
      args:
        BACKEND_URL: http://localhost:5001
    ports: !override
      - "8081:80"
